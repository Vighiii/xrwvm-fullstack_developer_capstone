applications:
  - name: best-cars-capstone
    memory: 512M
    disk_quota: 1G
    buildpacks:
      - python_buildpack
    command: python manage.py migrate --noinput && gunicorn djangoproj.wsgi:application --bind 0.0.0.0:$PORT
    env:
      DEBUG: "False"
